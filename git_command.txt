git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
git status
git config --list
git config --global credential.helper store
git add . 
git add filename.txt anotherfile.js
git add --all
git commit -m "Your descriptive commit message here"
git push origin master
git pull origin master

git diff                                -> if you have not yet run git add
git diff --staged                       -> after having run git add
git diff HEAD                           -> all changes in both staged and unstaged
git diff path/to/your/file.txt
git diff --staged path/to/your/file.txt
